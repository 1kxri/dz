int find_element(double* ptr_array, int n, double element)
{
    double eps = 0.0;
    for (int i = 0; i < n; i++)
    {
        if (fabs(ptr_array[i] - element) < eps)
            return i;
    }
    return -1;
}
